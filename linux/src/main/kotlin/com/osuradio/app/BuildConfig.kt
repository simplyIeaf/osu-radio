package com.osuradio.app

/**
 * Version constants shared by the whole desktop app.
 * Mirrors the values in `version.properties`.
 */
object BuildConfig {
    const val APP_VERSION: String = "1.4.0"
    const val VERSION_NAME: String = "1.4.0"
}
